import React from 'react';
import styled from '../utils/styled';

const SectionHeader = styled('header', 'flex justify-between items-start flex-wrap flex-row-reverse content-end');

export default SectionHeader;